from abc import ABC, abstractmethod
import datetime
import pandas as pd

class CryptoDockSuite(ABC) :

    def __init__(self) :
        super().__init__()

    def plot_orders_through_time() :
        pass
